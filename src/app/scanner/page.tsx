// app/scanner/page.tsx — vista del escáner (ruta dedicada para deep-linking)
import ScannerClient from "@/components/scanner/ScannerClient";

export default function Page() {
  return <ScannerClient />;
}